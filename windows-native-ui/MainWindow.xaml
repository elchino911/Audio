<Window x:Class="AudioLinkNativeUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Audio Link Native UI"
        Width="1120"
        Height="740"
        MinWidth="980"
        MinHeight="660"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip"
        Background="Transparent"
        Loaded="Window_Loaded">
    <Window.Resources>
        <SolidColorBrush x:Key="Bg" Color="#0C111A"/>
        <SolidColorBrush x:Key="Card" Color="#161E2C"/>
        <SolidColorBrush x:Key="Stroke" Color="#2D3950"/>
        <SolidColorBrush x:Key="TextMain" Color="#E6ECF8"/>
        <SolidColorBrush x:Key="TextSoft" Color="#ABB7D3"/>
        <SolidColorBrush x:Key="Primary" Color="#3B82F6"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
        </Style>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSoft}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#202B3D"/>
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Stroke}"/>
            <Setter Property="Padding" Value="9,6"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#202B3D"/>
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Stroke}"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Primary}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="13,7"/>
            <Setter Property="Margin" Value="0,0,8,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GhostButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#2B3C57"/>
            <Setter Property="BorderBrush" Value="#3D4E6B"/>
        </Style>
        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#7F1D1D"/>
            <Setter Property="BorderBrush" Value="#B91C1C"/>
        </Style>
        <Style x:Key="NavButtonStyle" TargetType="Button" BasedOn="{StaticResource GhostButtonStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="12,9"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource Card}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Stroke}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource Bg}" BorderBrush="{StaticResource Stroke}" BorderThickness="1.4" CornerRadius="18">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="56"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#111A2A" BorderBrush="{StaticResource Stroke}" BorderThickness="0,0,0,1" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="14,0,8,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Audio Link Native" FontSize="18" FontWeight="SemiBold"/>
                        <TextBlock x:Name="CurrentModeBadge" Text="  BASICO" Foreground="{StaticResource TextSoft}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <ComboBox x:Name="UiModeCombo" Width="120" SelectionChanged="UiModeCombo_SelectionChanged"/>
                        <ToggleButton x:Name="MuteInputToggle" Content="Mute Entrada" Margin="0,0,8,0" Checked="MuteInputToggle_Checked" Unchecked="MuteInputToggle_Unchecked"/>
                        <ToggleButton x:Name="MuteOutputToggle" Content="Mute Salida" Margin="0,0,8,0" Checked="MuteOutputToggle_Checked" Unchecked="MuteOutputToggle_Unchecked"/>
                        <Button Content="Refresh" Style="{StaticResource GhostButtonStyle}" Click="RefreshStatusButton_Click"/>
                        <Button Content="—" Width="30" Style="{StaticResource GhostButtonStyle}" Click="MinimizeButton_Click"/>
                        <Button Content="✕" Width="30" Style="{StaticResource DangerButtonStyle}" Click="CloseButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="205"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#0F1727" BorderBrush="{StaticResource Stroke}" BorderThickness="0,0,1,0" Padding="12,14">
                    <StackPanel>
                        <Button x:Name="NavDashboardButton" Content="Dashboard" Style="{StaticResource NavButtonStyle}" Click="NavDashboardButton_Click"/>
                        <Button x:Name="NavDownlinkButton" Content="Downlink" Style="{StaticResource NavButtonStyle}" Click="NavDownlinkButton_Click"/>
                        <Button x:Name="NavUplinkButton" Content="Uplink" Style="{StaticResource NavButtonStyle}" Click="NavUplinkButton_Click"/>
                        <Button x:Name="NavDiagnosticsButton" Content="Diagnostico" Style="{StaticResource NavButtonStyle}" Click="NavDiagnosticsButton_Click"/>
                        <Separator Margin="0,10,0,10"/>
                        <TextBlock Text="Workspace" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="WorkspaceBox"/>
                        <Button Content="Abrir Runtime Logs" Style="{StaticResource GhostButtonStyle}" Click="OpenRuntimeFolderButton_Click"/>
                        <Button Content="Start All" Style="{StaticResource ButtonStyle}" Click="StartAllButton_Click"/>
                        <Button Content="Stop All" Style="{StaticResource DangerButtonStyle}" Click="StopAllButton_Click"/>
                    </StackPanel>
                </Border>

                <Grid Grid.Column="1" Margin="14,14,14,10">
                    <Grid x:Name="DashboardView">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Resumen de Estado" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12"/>
                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <UniformGrid Rows="1" Columns="3" Margin="0,0,0,4">
                                <Border Style="{StaticResource CardStyle}" Margin="0,0,10,8">
                                    <StackPanel><TextBlock Text="Downlink" FontWeight="SemiBold"/><TextBlock x:Name="DownStatusText" Margin="0,8,0,4"/><TextBlock x:Name="DownStatusDetailText" Foreground="{StaticResource TextSoft}" TextWrapping="Wrap"/></StackPanel>
                                </Border>
                                <Border Style="{StaticResource CardStyle}" Margin="0,0,10,8">
                                    <StackPanel><TextBlock Text="Uplink Bridge" FontWeight="SemiBold"/><TextBlock x:Name="UpBridgeStatusText" Margin="0,8,0,4"/><TextBlock x:Name="UpBridgeStatusDetailText" Foreground="{StaticResource TextSoft}" TextWrapping="Wrap"/></StackPanel>
                                </Border>
                                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,8">
                                    <StackPanel><TextBlock Text="Uplink Mic" FontWeight="SemiBold"/><TextBlock x:Name="UpMicStatusText" Margin="0,8,0,4"/><TextBlock x:Name="UpMicStatusDetailText" Foreground="{StaticResource TextSoft}" TextWrapping="Wrap"/></StackPanel>
                                </Border>
                            </UniformGrid>
                            <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                                <WrapPanel>
                                    <Button Content="Start Downlink" Style="{StaticResource ButtonStyle}" Click="StartDownlinkButton_Click"/>
                                    <Button Content="Stop Downlink" Style="{StaticResource GhostButtonStyle}" Click="StopDownlinkButton_Click"/>
                                    <Button Content="Start Uplink" Style="{StaticResource ButtonStyle}" Click="StartUplinkButton_Click"/>
                                    <Button Content="Stop Uplink" Style="{StaticResource GhostButtonStyle}" Click="StopUplinkButton_Click"/>
                                </WrapPanel>
                            </Border>
                            <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,0">
                                <TextBox x:Name="QuickActionOutputBox" IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" Background="#111A2A"/>
                            </Border>
                        </Grid>
                    </Grid>

                    <Grid x:Name="DownlinkView" Visibility="Collapsed">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="Downlink (Windows -> Android)" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12"/>
                                <Border Style="{StaticResource CardStyle}">
                                    <Grid>
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                            <TextBlock Text="Mode" Style="{StaticResource LabelStyle}"/><ComboBox x:Name="DownModeCombo"/>
                                            <TextBlock Text="Target IP" Style="{StaticResource LabelStyle}"/><TextBox x:Name="DownTargetIpBox"/>
                                            <TextBlock Text="Source" Style="{StaticResource LabelStyle}"/><ComboBox x:Name="DownSourceCombo" SelectionChanged="DownSourceCombo_SelectionChanged"/>
                                            <TextBlock Text="Desktop Device" Style="{StaticResource LabelStyle}"/>
                                            <DockPanel Margin="0,0,0,10"><Button x:Name="ReloadDesktopDevicesButton" DockPanel.Dock="Right" Content="Reload" Style="{StaticResource GhostButtonStyle}" Click="ReloadDesktopDevicesButton_Click" Margin="8,0,0,0"/><ComboBox x:Name="DownDesktopDeviceCombo" IsEditable="True"/></DockPanel>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" x:Name="DownAdvancedPanel">
                                            <TextBlock Text="Profile" Style="{StaticResource LabelStyle}"/><ComboBox x:Name="GlobalProfileCombo"/>
                                            <TextBlock Text="Device Serial" Style="{StaticResource LabelStyle}"/><TextBox x:Name="DeviceSerialBox"/>
                                            <TextBlock Text="Port" Style="{StaticResource LabelStyle}"/><ComboBox x:Name="DownPortCombo"/>
                                            <TextBlock Text="Frame ms" Style="{StaticResource LabelStyle}"/><ComboBox x:Name="DownFrameCombo"/>
                                            <TextBlock Text="Jitter ms" Style="{StaticResource LabelStyle}"/><ComboBox x:Name="DownJitterCombo"/>
                                            <TextBlock Text="Transport" Style="{StaticResource LabelStyle}"/><ComboBox x:Name="DownTransportCombo"/>
                                            <TextBlock Text="Auto reconnect USB" Style="{StaticResource LabelStyle}"/><ComboBox x:Name="DownAutoReconnectCombo"/>
                                            <TextBlock Text="USB watchdog ms" Style="{StaticResource LabelStyle}"/><TextBox x:Name="DownUsbWatchdogBox"/>
                                            <WrapPanel><CheckBox x:Name="DownSkipBuildCheck" Content="Skip build" Margin="0,0,12,0"/><CheckBox x:Name="DownSkipReceiverCheck" Content="Skip Android receiver"/></WrapPanel>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,0"><WrapPanel><Button Content="Start Downlink" Style="{StaticResource ButtonStyle}" Click="StartDownlinkButton_Click"/><Button Content="Stop Downlink" Style="{StaticResource DangerButtonStyle}" Click="StopDownlinkButton_Click"/></WrapPanel></Border>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>

                    <Grid x:Name="UplinkView" Visibility="Collapsed">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="Uplink (Android mic -> Windows)" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12"/>
                                <Border Style="{StaticResource CardStyle}"><StackPanel><TextBlock x:Name="UplinkBasicHintText" Foreground="{StaticResource TextSoft}" TextWrapping="Wrap" Margin="0,0,0,8"/><WrapPanel><Button Content="Start Uplink" Style="{StaticResource ButtonStyle}" Click="StartUplinkButton_Click"/><Button Content="Stop Uplink" Style="{StaticResource DangerButtonStyle}" Click="StopUplinkButton_Click"/></WrapPanel></StackPanel></Border>
                                <Border x:Name="UplinkAdvancedPanel" Style="{StaticResource CardStyle}" Margin="0,0,0,0">
                                    <Grid>
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                            <TextBlock Text="Target IP" Style="{StaticResource LabelStyle}"/><TextBox x:Name="UpTargetIpBox"/>
                                            <TextBlock Text="Port" Style="{StaticResource LabelStyle}"/><ComboBox x:Name="UpPortCombo"/>
                                            <TextBlock Text="Frame ms" Style="{StaticResource LabelStyle}"/><ComboBox x:Name="UpFrameCombo"/>
                                            <TextBlock Text="Transport" Style="{StaticResource LabelStyle}"/><ComboBox x:Name="UpTransportCombo"/>
                                            <TextBlock Text="Mic source" Style="{StaticResource LabelStyle}"/><ComboBox x:Name="UpMicSourceCombo"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="Output Device" Style="{StaticResource LabelStyle}"/><TextBox x:Name="UpOutputDeviceBox"/>
                                            <TextBlock Text="Target Buffer ms" Style="{StaticResource LabelStyle}"/><ComboBox x:Name="UpTargetBufferCombo"/>
                                            <TextBlock Text="Max Buffer ms" Style="{StaticResource LabelStyle}"/><ComboBox x:Name="UpMaxBufferCombo"/>
                                            <WrapPanel><CheckBox x:Name="UpSkipBuildBridgeCheck" Content="Skip bridge build" Margin="0,0,12,0"/><CheckBox x:Name="UpNoRestartMicCheck" Content="No restart mic" Margin="0,0,12,0"/><CheckBox x:Name="UpStartAndroidMicCheck" Content="Start Android mic" IsChecked="True"/></WrapPanel>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>

                    <Grid x:Name="DiagnosticsView" Visibility="Collapsed">
                        <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="Action Output" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <TextBox x:Name="ActionOutputBox" IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" Background="#111A2A"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,0">
                            <Grid>
                                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                                <WrapPanel Margin="0,0,0,8"><TextBlock Text="Profile" Style="{StaticResource LabelStyle}" Margin="0,6,6,0"/><ComboBox x:Name="LogsProfileCombo" Width="120"/><TextBlock Text="Tail" Style="{StaticResource LabelStyle}" Margin="8,6,6,0"/><ComboBox x:Name="LogsTailCombo" Width="90"/><Button Content="Reload Logs" Style="{StaticResource GhostButtonStyle}" Click="ReloadLogsButton_Click"/></WrapPanel>
                                <TextBox Grid.Row="1" x:Name="LogsOutputBox" IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" Background="#111A2A"/>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>

            <Grid x:Name="LoadingOverlay" Grid.RowSpan="2" Background="#A20B111A" Visibility="Collapsed">
                <Border Width="340" Height="150" Background="#182234" BorderBrush="{StaticResource Stroke}" BorderThickness="1" CornerRadius="14" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <StackPanel Margin="20"><TextBlock x:Name="LoadingText" Text="Inicializando..." FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,12"/><ProgressBar IsIndeterminate="True" Height="10"/></StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
